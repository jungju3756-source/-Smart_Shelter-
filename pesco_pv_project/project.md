1) 파이선 패키지 panda3d 를 사용하여 3차원 오피스 공간을 만들고 여기에 plc, 에어콘, 카메라 view를 배치하고 카메라 뷰에 컴퓨터에 연결된  usb camera의 영상을 실시간으로 보여줌
2) plc에는 5초마다 랜덤으로 전압, 전류 값을 생성하고 이를 plc에 부착된 인디케이터에 표시하고 에어콘에도 on/off 인디케이터를 부착해줘
3) 윈도에 설치된 usb camera로부터의 영상에 yolo 를 사용하여 사람을 검출하고 사람이 검출되면 에어콘에 부착된 on/off 인디케이터를 on 시키고 사람이 없으면 off 로 변경함
4) 3번에서 객체검출되는 화면을 camera view에 실시간으로 표시해줘
5) 파이선 작업은 uv로 생성된 .venv의 가상환경에서 실행시키려고 해
